#!/bin/sh
# ~/.xsessionrc - xorg settings
#
# debian: ~/.xsessionrc
# arch: ~/.xprofile

# Turn off speaker - since zsh i don't need it.
xset b off

# Ctrl to CapsLock
setxkbmap -option ctrl:nocaps

# For some reasons Xdefaults does not read without this.
xrdb ~/.Xresources
