return function(use)
  -- Using mini.nvim's comment, but maybe this has better LSP support
  -- use {
  --   'numtostr/comment.nvim',
  --   config = function()
  --       require('Comment').setup()
  --   end
  -- }
end
